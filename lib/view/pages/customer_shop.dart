import 'package:flutter/material.dart';

class CustomerShop extends StatelessWidget {
  const CustomerShop({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is Customer Shop Page.'));
  }
}
