import 'package:flutter/material.dart';

class CustomerCart extends StatelessWidget {
  const CustomerCart({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This is Customer Cart Page.'));
  }
}
